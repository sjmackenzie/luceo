# Luceo
(interim name)

## Build

`$ git clone`

`$ git submodule update --init --recursive`

`$ nix-shell` or install:

```
libui
pkgconfig
clang
glibc
cmake
rustup
```

`$ rustup run nightly cargo run`

